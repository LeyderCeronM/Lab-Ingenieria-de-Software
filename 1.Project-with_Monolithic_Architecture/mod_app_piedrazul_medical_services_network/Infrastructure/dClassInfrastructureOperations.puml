@startuml InfrastructureOperationsClasses

interface IUserRepository <<interface>> #aliceblue;line:blue;text:blue {
  ' --- existente ---
  +verifyUser(String username, String password): Role
  ' --- E1-HU1: Registrar Usuario ---
  +opSave(ClsUser user): ClsUser
  +opExistsByUsername(String username): boolean
  ' --- E1-HU2: Actualizar Usuario ---
  +opUpdate(ClsUser user): boolean
  +opExistsByUsernameExcludingId(String username, long id): boolean
  ' --- E1-HU3: Consultar Usuarios ---
  +opFindAll(): List<ClsUser>
  +opGet(long id): ClsUser
  ' --- E1-HU4: Desactivar Usuario ---
  +opDeactivate(long id): boolean
  +opUpdateState(long id, int stateId): boolean
  ' --- (no soportado, lanza excepción) ---
  +opDelete(long id): boolean
}

class SQLiteUserRepository <<repository>> #PaleGreen;line:Black;text:Black {
  ' --- existente ---
  +verifyUser(String username, String password): Role
  ' --- E1-HU1: Registrar Usuario ---
  +opSave(ClsUser user): ClsUser
  -savePatient(Connection conn, ClsPatient patient): void
  -saveMedicalStaff(Connection conn, ClsClinicalStaff staff): void
  +opExistsByUsername(String username): boolean
  ' --- E1-HU2: Actualizar Usuario ---
  +opUpdate(ClsUser user): boolean
  -updatePatient(Connection conn, ClsPatient patient): void
  -updateMedicalStaff(Connection conn, ClsClinicalStaff staff): void
  +opExistsByUsernameExcludingId(String username, long id): boolean
  ' --- E1-HU3: Consultar Usuarios ---
  +opFindAll(): List<ClsUser>
  +opGet(long id): ClsUser
  ' --- E1-HU4: Desactivar Usuario ---
  +opDeactivate(long id): boolean
  +opUpdateState(long id, int stateId): boolean
  ' --- (no soportado) ---
  +opDelete(long id): boolean
}

@enduml
